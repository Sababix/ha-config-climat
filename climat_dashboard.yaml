type: vertical-stack
cards:
  - type: custom:mushroom-title-card
    title: >-
      🎄 {{ 'Bonsoir' if now().hour > 17 else 'Bonjour' }} {{ user.split('
      ')[-1] }} 🎅
    alignment: center
    card_mod:
      style: |
        ha-card {
          --title-font-size: 24px !important;
          --subtitle-font-size: 14px !important;
          margin-top: -10px;
        }
  - type: custom:mushroom-chips-card
    chips:
      - type: weather
        entity: weather.toulon
        show_conditions: true
        show_temperature: true
      - type: template
        entity: sensor.temperature_sejour
        icon: mdi:sofa
        icon_color: orange
        content: "{{ states(entity) | round(1) }}°C"
        tap_action:
          action: more-info
      - type: template
        entity: sensor.temperature_chambre
        icon: mdi:bed
        icon_color: blue
        content: "{{ states(entity) | round(1) }}°C"
        tap_action:
          action: more-info
      - type: template
        entity: sensor.temperature_couloir
        icon: mdi:walk
        icon_color: grey
        content: "{{ states(entity) | round(1) }}°C"
        tap_action:
          action: more-info
    alignment: center
    card_mod:
      style: |
        ha-card {
          --chip-background: rgba(var(--rgb-primary-text-color), 0.05);
          margin-bottom: 20px;
        }
  - type: custom:mushroom-climate-card
    entity: climate.clim_salon
    name: Salon
    icon: mdi:sofa
    hvac_modes:
      - heat
      - cool
      - dry
      - "off"
    show_temperature_control: true
    collapsible_controls: true
    layout: horizontal
    fill_container: true
    primary_info: name
    secondary_info: state
    tap_action:
      action: toggle
  - type: custom:mushroom-climate-card
    entity: climate.clim_sam
    name: Salle à Manger
    icon: mdi:table-chair
    hvac_modes:
      - heat
      - cool
      - dry
      - "off"
    show_temperature_control: true
    collapsible_controls: true
    layout: horizontal
    fill_container: true
  - type: custom:mushroom-climate-card
    entity: climate.clim_chambre
    name: Chambre
    icon: mdi:bed
    hvac_modes:
      - heat
      - cool
      - dry
      - "off"
    show_temperature_control: true
    collapsible_controls: true
    layout: horizontal
    fill_container: true
  - type: custom:mushroom-template-card
    primary: Consommation Climatisation
    secondary: |
      {{ (states('sensor.clim_salon_energie') | float(0) + 
          states('sensor.clim_sam_energie') | float(0) + 
          states('sensor.clim_chambre_energie') | float(0)) | round(1) }} kWh
    icon: mdi:lightning-bolt
    icon_color: yellow
    layout: horizontal
    tap_action:
      action: none
